spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/url_shortener_db
    username: postgres
    password: root123

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

server:
  port: 8080